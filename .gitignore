venv/
__pycache__/
*.pyc
*.pyo
.env
storage/
test/results/
*.egg-info/
dist/
.pytest_cache/
.mypy_cache/
protos/generated/*_pb2*.py

# 모델 디렉토리 (대용량 파일 — git 제외)
/models/

# 모델 가중치 파일 안전망 (models/ 밖에 실수로 저장된 경우 방지)
*.safetensors
*.gguf
*.ggml

# 테스트 리포트·로컬 E2E 산출물 (생성물·저작권 슬라이스 — git 제외)
test_report.html
test/report/
test/data/

# 런타임 잡 산출물 (페이지별 중간·최종 결과 — git 제외)
output/

# 코드에서 참조하지 않는 잔재 샘플 (실제 경로는 test/samples/test.pdf)
images/test.pdf
