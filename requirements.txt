# =================================================================
# Semojum V2 AI — requirements.txt  (Stage 1 핵심 패키지)
# 환경: Ubuntu 22.04 / Python 3.10 / CUDA 12.1 / L4 GPU
#
# [중요] 이 파일은 Stage 1 gRPC 인프라 구동에 필요한 패키지만 포함한다.
#        torch, torchvision, autoawq 는 setup.sh 에서 직접 설치.
#        Stage 2+ AI 모델 의존 패키지는 requirements-ai.txt 를 참조.
# =================================================================

# ── gRPC ──────────────────────────────────────────────────────────
grpcio==1.67.1
grpcio-tools==1.67.1
protobuf==5.28.3             # grpcio 1.67.x 와 호환

# ── API / 서버 ────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12    # FastAPI 파일 업로드

# ── 데이터 검증 ───────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ── 수치 연산 ─────────────────────────────────────────────────────
numpy==1.26.4                # [CRITICAL] <2.0.0 고정 — torch 2.5.1 호환

# ── 이미지 / PDF 처리 ─────────────────────────────────────────────
Pillow==10.4.0
opencv-python-headless==4.10.0.84
PyMuPDF==1.24.14            # ZERO Tier 텍스트 추출

# ── 기타 유틸 ─────────────────────────────────────────────────────
aiofiles==24.1.0
requests==2.32.3

# ── 개발·테스트 ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
